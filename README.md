# Pass-Master-RAG 🎓

정보처리기사 실기 합격을 위한 데이터 추출 및 RAG 시스템입니다.

## 🛠 세팅 방법 (Setup)
1. 가상환경 생성 및 활성화:
   `python -m venv venv`
   `source venv/Scripts/activate` (Windows)
2. 패키지 설치:
   `pip install -r requirements.txt`